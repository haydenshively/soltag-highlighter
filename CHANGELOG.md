# Changelog

## 0.0.1

- Initial release
- Solidity syntax highlighting inside `sol` tagged template literals
- Support for `${...}` interpolations with JS/TS highlighting
- Support for `sol(...)` call syntax
